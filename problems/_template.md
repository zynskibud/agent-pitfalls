# NNN — Short slug

**Status:** open / fix in progress / fixed / wontfix
**Surfaces in:** Claude Code / Agent SDK / Cursor / etc.
**Fix repo:** (link if exists, otherwise leave blank)

---

## Observation

What you see, with concrete examples. A repro path if possible. Don't hand-wave — say *exactly* what the agent did and what it should have done.

## Why this happens (best guess)

Your model of the root cause. Where in the harness the gap is. What the model would need access to that it doesn't currently have. Be honest about what's a guess vs. what's verified.

## What the fix looks like

The shape of the fix, not necessarily the implementation. Where it should live (in the binary / in user-space hook / in the SDK / in a standalone wrapper). How invasive it is.

## Why this matters

Not "this is annoying" — **what class of work the agent can't do until this is fixed.** What does the user have to babysit? What does the model get confidently wrong because of this?

## Open questions

Things you're not sure about. Stuff that would change your analysis if answered.
